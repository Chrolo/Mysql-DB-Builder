{
    "type": "object",
    "description": "The properties required to define an SQL table.",
    "properties": {
        "name":{
            "type": "string",
            "description": "the name of the table"
        },

        "fields": {
            "type": "array",
            "description": "the fields of the table",
            "items": {
                "$ref": "field.json#"
            }
        },

        "primaryKey" :{
            "type": "string",
            "description": "The field name of the primary key"
        }
    },
    "required":[
        "name",
        "fields",
        "primaryKey"
    ]
}
