{
    "id": "database.json",
    "type": "object",
    "description": "The properties of a database (from a data perspective).",
    "properties": {
        "name": {
            "type":"string",
            "description": "the name of the database"
        },
        "tables": {
            "type": "array",
            "items": {
                "$ref": "table.json#"
            }
        }
    },
    "required": [
        "tables"
    ]
}
