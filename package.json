{
  "name": "dbbuilder",
  "version": "1.0.0",
  "description": "A utility to create SQL tables from JSON definitions. Will additionally insert initialisation data if wanted.",
  "main": "src/main.js",
  "directories": {
    "doc": "docs"
  },
  "bin": {
    "mysqlBuilder": "./src/main.js"
  },
  "scripts": {
    "test": "mocha -c src/**/*.spec.js",
    "lint": "eslint ./src/**/*.js",
    "lint:fix": "eslint ./src/**/*.js --fix"
  },
  "author": "Chrolo",
  "license": "UNLICENSED",
  "dependencies": {
    "ajv": "^5.2.2",
    "mysql": "^2.14.1",
    "yargs": "^8.0.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.8.0",
    "mocha": "^3.5.0"
  }
}
